extends layouts/main-layout.pug

block content    
    script. 
        var postId='!{postId}'
        var userLoggedIn=!{userLoggedInJs} 
    .postsContainer
    +createPostModals(userLoggedIn)
block script 
    script(src="/common/postPage.js") 